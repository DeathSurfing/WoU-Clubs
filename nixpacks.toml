[phases.setup]
nixPkgs = ["bun", "nodejs_20"]

[phases.install]
cmds = ["bun install --no-save"]

[phases.build]
cmds = ["node ./node_modules/.bin/next build"]

[phases.start]
cmd = "node ./node_modules/.bin/next start -p $PORT"
env = { PORT = "3000" }
